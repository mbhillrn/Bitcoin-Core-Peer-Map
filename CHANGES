Multi-peer dot interaction with grouped hover/click/drill-down. AS Diversity sub-tooltip with expandable peer lists, pinned filtering, and clickable peer IDs that zoom to map location. Debug terminal UI polish: update retry, scrollback, Antarctica modal, startup cleanup, legend hover suppression, and map hover panel fix.
