Connection lines now prefer visible peers on screen over off-screen ones. Hover preview shows lines and filters the peer table across all panel rows. Fixed drill-down state being destroyed by the data refresh cycle.
